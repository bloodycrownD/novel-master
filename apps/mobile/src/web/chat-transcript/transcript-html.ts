/**
 * ChatTranscriptWebView HTML 导出面：Metro/Jest 将组装产物读为字符串 + BASE_URL。
 */
import CHAT_TRANSCRIPT_HTML from './transcript.assembled.html';

export { CHAT_TRANSCRIPT_HTML };

/** WebView `source.baseUrl`，与历史契约一致。 */
export const CHAT_TRANSCRIPT_BASE_URL = 'https://novel-master.local/';
