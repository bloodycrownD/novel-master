/**
 * Desktop 入口：发送门闩经 `@shared/logic/chat` 再导出（单源在 core）。
 */
export {
  resolveComposerSendIntent,
  type ComposerSendIntent,
  type ComposerSendIntentAttachment,
  type ComposerSendIntentInput,
} from "@shared/logic/chat";
