/**
 * Desktop renderer 对 `@novel-master/core/prompt` 的具名薄再导出。
 * 禁止 `export *`。
 */

export type {
  DynamicPromptBlock,
  PersistPromptBlock,
  PersistTextPromptBlock,
} from "@novel-master/core/prompt";

export { ALLOWED_DYNAMIC_ROOT_MACROS } from "@novel-master/core/prompt";
