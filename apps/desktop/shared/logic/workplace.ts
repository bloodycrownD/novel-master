/**
 * Desktop renderer 对 `@novel-master/core/workplace` 的具名薄再导出。
 * 禁止 `export *`；禁止 createWorkplaceService 等工厂。
 */

export {
  DEFAULT_WORKPLACE_DIR_RULE,
  displayStateLabel,
  inclusionModeLabel,
  ruleStateLabel,
} from "@novel-master/core/workplace";
