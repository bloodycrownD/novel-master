---
date: 2026-09-06 23:35
title: glm-5.3 上下文窗口 override 调整 400k→500k
keywords: models.json, glm-5.3, contextWindow, zai, override
abstract: 应用户要求把 ~/.pi/agent/models.json 里 zai 与 zai-coding-cn 两个 provider 的 glm-5.3 contextWindow 从 400000 改为 500000。
---

user:
帮我调整 glm 模型的上下文，现在是 400k，改成 500k 吧。

assistant:
改的是 `~/.pi/agent/models.json`（手动 override，非内置目录值）：zai 与 zai-coding-cn 两个 provider 的 glm-5.3 `contextWindow` 都从 400000 调到 500000，JSON 校验通过。生效方式：打开 /model 时热加载；已在跑的会话沿用旧值。magic-context 的阈值随窗口自动缩放（500k × 65% 执行阈值 ≈ 325k，主动触发 ≈ 315k），无需改它的配置。注意：若智谱 API 实际上限低于 500k，超限请求才会靠 pi 的溢出恢复兜底，属用户自担的 override。
