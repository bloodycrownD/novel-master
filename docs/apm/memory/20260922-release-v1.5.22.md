---
date: 2026-09-23 00:15
title: v1.5.22 发版：存储缓存去重与数据清理
keywords: release, v1.5.22, 数据清理, 缓存去重, VACUUM, storage-cache-dedup-and-cleanup
abstract: v1.5.22 发布（tag 6e7f8ed2 Release commit，main 13549791→6e7f8ed2）。内容：提示词缓存去重存储（session_file_cache_blob/entry + 清空迁移）+ 双端数据清理（体积展示/GC/VACUUM）+ mobile 存储页重排与云端存储设置页 + CR 修复十条 + e2e 资产 Windows 适配。bump 时 build.gradle 遭 utf8 往返破坏 GBK 注释，git checkout 后改 Buffer 字节级替换修复——非 UTF-8 文件的程序化替换必须走 Buffer indexOf，不经字符串编解码。
---

user:
$novel-master-publish +0.0.1 是 1.5.22 吧？（确认发版）

assistant:
v1.5.22 发版完成（2026-09-23 零点）：feat/storage-cache-dedup-and-cleanup 与 test/desk-e2e-maintenance 两笔 --no-ff 合入 main（a575caae/20d11b17），bump 五文件（CHANGELOG [1.5.22] + 双端 package.json + build.gradle + lock，六行 diff）后 Release commit 6e7f8ed2、tag v1.5.22 已推送触发三平台 Release CI、main 已同步。教训一条：build.gradle 含 GBK 中文注释，node utf8 字符串往返会把无效字节换成 U+FFFD 静默破坏注释——非 UTF-8 文件的替换必须 Buffer.indexOf 字节级操作（本次靠 git checkout 恢复后重做）。下一版基线 v1.5.22。
