---
date: 2026-09-06 19:04
title: feature-optimizations-2026-09 迭代两 feature execute-ready 审查（第 1 轮）
keywords: execute-ready, workspace-push, mobile-charts-redo, 代码评审, PRD, SPEC, replaceVfsSubtree, copyScope, bumpWorktreeUiToken, StackedBars, PieChart
abstract: 迭代总纲下 workspace-push 与 mobile-charts-redo 两个 feature 的 PRD+SPEC 第 1 轮 execute-ready 只读评审。全部 spec 引用逐条对照代码验证（行号、符号、链路、测试断言面均核实），两 feature 均 Go：无 P0/P1；workspace-push 3 条 P2（notify payload 的 projectId 来源未写明、PRD「不离开当前页即可见刷新」与 spec 互斥面板口径需对齐、子会话 gate 可直接用 ShellNavContext 已暴露的 subagentSessionId 无需新增传递链）；mobile-charts-redo 1 条 P2（stacked-bars.test.tsx 的 T-MB1 居中/T-MB2 超宽滚动两个布局断言用例的处置未在测试策略中点名，测量点上移后 layoutContainer 辅助与 minWidth 断言值需适配）。
---

user:
以 readonly 模式审查 feature-optimizations-2026-09 的 workspace-push 与 mobile-charts-redo 两 feature 的 PRD+SPEC 是否 execute-ready（第 1 轮，无上轮 must-fix），要求先列阅读清单、对照代码库验证 spec 引用（符号名/签名/路径/行号）、检查矛盾/缺失契约/验收可测性/Step 标注格式/T-用例映射/测试策略遗漏，分 feature 给 Go/No-Go，禁止改文件、禁止仅凭文档下结论。

assistant:
（主代理手工只读探索）阅读清单：迭代总纲与两 feature 的 prd.md/spec.md；core 侧 template-pull.port.ts、impl/template-pull.service.ts、create-template-pull-service.ts、logic/initialize-session-workspace.ts、vfs-tree-copy.ts（replaceVfsSubtree:338）、sqlite-workplace.repository.ts（copyScope:238）、workplace-path-map.ts、session.port.ts/impl（pullTemplate:53/226、get）、test/workplace/template-pull.test.ts、test/vfs/vfs-gc-trigger.test.ts；desktop 侧 shared/ipc-types.ts（SESSIONS_PULL_TEMPLATE:82/Request:622）、handlers/sessions.ts:91、handler-registry.ts:145/227、invoke-registry.ts:303、client.ts:83、WorkspaceHeaderActions.tsx、resolve-vfs-scope.ts（'session'→core project 双关确认）、forward-workspace-mutated.ts、ShellNavProvider.tsx（workspaceMutatedMatchesNav + subagentSessionId 已在 context）、ExplorerPane.tsx（PANEL_SCOPES 互斥渲染）；mobile 侧 TemplatePullButton.tsx、VfsFileManager.tsx（pullFromParent）、ChatConversationPanel.tsx:317/322（key=session-vfs-${vfsRefreshKey}）、ChatSessionListPanel.tsx:134（key=project-template-${vfsRefreshKey}，天然双刷确认）、useChatTabScope.ts:334（bumpWorktreeUiToken=setVfsRefreshKey+1）、TabIcons.tsx:217（SyncPullIcon）、SubagentSessionScreen.tsx（无工作区面板确认）；charts 侧 StackedBars.tsx/PieChart.tsx 全文、SummaryTab/DetailTab/styles.ts、core/common/format-token-count.ts（1500→1.5K/750→750，desktop 镜像注释确认）、__tests__/stacked-bars.test.tsx、__tests__/token-usage-stats-screen.test.tsx（svg mock 仅 default/Path/Circle）、apps/mobile/package.json（react-native-svg ^15.15.5 无图表库）。

结论：两 feature 均 Go（execute-ready），无 P0/P1。关键验证：replaceVfsSubtree 方向无关且行号精确；copyScope 先 deleteScope 再 upsert 覆盖语义；mapSessionWorkplacePathToProject 现成恒等；SessionService.get(id) 可供 push handler 取 projectId 做 notify；desktop IPC 五环节全在位；notify {workspaceScope:'session',projectId} 命中 workspaceMutatedMatchesNav 的 project 面板分支（只比 projectId）；bumpWorktreeUiToken 经 vfsRefreshKey 同时驱动 session-vfs- 与 project-template- 两个面板 key（双刷属实）；子会话 gate 双端现状与 spec 描述一致（desktop ExplorerPane 不区分子会话视图故「初始化」可见、ShellNavContextValue 已暴露 subagentSessionId；mobile SubagentSessionScreen 无工作区面板）；StackedBars gridLayer 确嵌 ScrollView 内容层、⅓/⅔ 两线+grid-max-label 现状与 spec 探索一致；PieChart 扇区按 data 内部 sum 与详情行按传入 totalTokens 的双轨分母属实；T-MC1 示例值（1500→1.5K/750/0）与 formatTokenCount 行为吻合；template-pull.test.ts:98 有删除侧断言、vfs-gc-trigger T-G2 有无 orphan blob 模式。

P2（workspace-push）：①Step3/T-WP6 notify 需 projectId 但 Request={sessionId}，spec 未写来源（路径现成：rt.sessions.get(sessionId).projectId）；②PRD「不离开当前页即可见项目工作区面板已刷新」需按 spec 风险段口径（互斥面板、切换后重挂载即见）执行，两文档措辞间留了对齐空间；③spec「由 ExplorerPane/ShellNavProvider 传视图信息」可简化——context 已暴露 subagentSessionId，WorkspaceHeaderActions 已用 useShellNav，直接读即可。P2（mobile-charts-redo）：stacked-bars.test.tsx 的 T-MB1（居中/minWidth=320）/T-MB2（超宽滚动/bar 宽 18）属布局断言，既非「网格用例」也非「交互断言」，spec 测试策略未点名处置；测量点上移 ScrollView 后 layoutContainer 辅助函数与 minWidth 断言值需适配（语义保持）。另提不阻塞建议：PRD 风险条目的 CLI template-pull e2e 核实在 spec 阶段以 core 单测断言+dist 陈旧诊断替代，若要完全闭环可在实现阶段 build core 后补跑一次 CLI e2e（可选）。
