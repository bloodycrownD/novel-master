---
date: 2026-09-04 00:05
title: novel-master 对外介绍文案优化：精简版与完整版
keywords: 介绍, 文案, 产品介绍, README, 营销, novel-master
abstract: 用户觉得原介绍写少了功能（"Rule 文件都比这个多"），基于 README 功能清单产出精简版（贴原结构润色）与完整版（三端/多智能体/批注/压缩置位/回滚/统计全列）两版文案；缓存命中率加"连续对话中"限定以立得住。
---

user:
帮我优化下面的介绍，可以增减描述，其实我感觉我功能甚至写少了，RULE 文件都比这个多：一个专注于小说创作的 agent 工具，自带工作区管理文件，自由控制每个文件在上下文中的展示。还支持 skill 标准，可以直接导入现成的 skill。同样支持智能体构建，让不同的功能由不同智能体负责，不再需要一个预设走天下。缓存命中率优化，不会搭配预设也能轻松有 90% 的缓存命中率。

assistant:
通读 README（109 行，功能清单很全）后产出两版：精简版贴原结构润色——"自带工作区管理文件"改为"统一放进工作区，Agent 直接读写"（避免读成文件管理器），缓存句加"连续对话中"限定（当天刚验证：密集连续 96~100%、隔时过期会掉，限定后立得住）；完整版补齐 README 实有功能——三层工作区、目录级展示规则、多智能体+工具允许/拒绝名单+子代理、Skill 标准、缓存优化、压缩/置位、批注定点改稿、Session FS 批次回滚、用量统计页、三端（Win/macOS/Linux + Android + nm CLI）。已提示可按投放位置（应用商店/群公告/README 顶部）再定制。

user:
90% 命中率不算骗人吧？我觉得主要是使用次数太少，如果像 coding 场景一样几小时数百次请求，缓存命中率会很高。

assistant:
核算结论：不算骗人，但要带"连续对话"限定。实测密集段 29 通加权 93.1%（96~100%）；模拟 coding 密度（300 通摊 4 次过期）按 token 加权约 95.7%，90%+ 成立。小说场景两个特殊因素：上下文 12~15 万 token，一次过期重读惩罚被放大；过期由服务商 TTL 决定（智谱实测 3.5~18 分钟），产品设计管不了，能控的（前缀冻结、回合快照、工作区常驻）已做到。文案定稿口径：保留"连续对话 90%+ 前缀缓存命中"，也可把归因挪到机制（"工作区前缀稳定，连续创作时缓存命中天然 90%+"）。

user:
README 也重写吧，结合下之前那个介绍。

assistant:
README.md 重写：定位语改为「专注长篇小说创作的 AI Agent 工作台」，导语融入介绍文案，新增「亮点」六条（上下文规则/多智能体/Skill 标准/缓存命中/长对话/用量统计——缓存条沿用"连续对话中 90%+"定稿口径）；正文新增「Skill 扩展」（SKILL.md 标准、ZIP 导入、管理界面——已核实 packages/core/src/service/skills 与两端 Skill 管理屏）与「数据统计」两节（desktop 设置 / mobile 我的页入口、按日/小时桶、服务商×模型筛选、请求流水）；「Agent 与工具」更名「智能体与工具」，典型流程补 skill 导入与多智能体一步；截图/交流群/许可证/开发等段落原样保留。CHANGELOG 未动（docs-only 变更不进版本记录）。
