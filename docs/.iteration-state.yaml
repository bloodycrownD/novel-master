# import-cache-align-and-mermaid-error 开发 DAG 编排状态（code-dev-loop）

spec_path: docs/Iterations/import-cache-align-and-mermaid-error/spec.md
prd_path: docs/Iterations/import-cache-align-and-mermaid-error/prd.md
branch: feat/import-cache-mermaid-error
base_sha: 2f1c4fc（= main）
status: in-progress（wave-0 impl 并行中）

dag_version: 1
wave_plan: [[impl-core, impl-desktop, impl-mobile], [verify-core, verify-desktop, verify-mobile], [cr-func-all]]
node_status:
  impl-core: { status: pending, executor: subagent }    # Step 1：helper + 双 service 接入 + 工厂 + T-IC1-5
  impl-desktop: { status: pending, executor: subagent } # Step 2：MermaidMarkdown 错误缓存 + T-MD1-3
  impl-mobile: { status: pending, executor: subagent }  # Step 3：mermaid-core catch + CSS attr + T-MV1-2
  verify-core: { status: pending }
  verify-desktop: { status: pending }
  verify-mobile: { status: pending }
  cr-func-all: { status: pending }

blocking_steps: [Step 1, Step 2, Step 3]   # Step 4 = manual_user 不阻塞
contracts:
  - clearSessionPromptCaches(sessionId, sessionKkv) helper，整体 try/catch 吞错
  - 工厂对外签名 (conn, options?) 不变；sessionKkv 工厂内部 createSessionKkvService(conn) 自建
  - kind 门闸 scope.kind === "session"（保护 meta-scope）
  - lookupMermaidFailedError(theme, source) 导出
  - mobile data-mermaid-error dataset + CSS attr(data-mermaid-error, fallback)
spec_deviations: []
open_must_fix: []

备注：
- 并行子代理禁 git commit；主代理按节点分组统一提交
- 主 worktree 用户未提交改动（.vscode 删除、AGENTS.md、package-lock.json）与 core/test/skills untracked——永不触碰、永不提交
