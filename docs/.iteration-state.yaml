# token-usage-stats 开发 DAG 编排状态（code-dev-loop）
# spec: docs/Iterations/token-usage-stats/spec.md（execute-ready 已确认）
# branch: feat/token-usage-stats（base aa32a19）

spec_path: docs/Iterations/token-usage-stats/spec.md
prd_path: docs/Iterations/token-usage-stats/prd.md
branch: feat/token-usage-stats
dag_version: 1
wave_plan: [
  [impl-s1-schema],
  [impl-s2-parse, impl-s3-persist],
  [impl-s4-backfill, impl-s5-stats],
  [verify-core],
  [cr-func-core],
  [impl-s6-desktop-ipc, impl-s8-mobile],
  [impl-s7-desktop-view],
  [verify-ui],
  [cr-func-final]
]
node_status:
  impl-s1-schema: { status: pending, step: Step1, executor: subagent }
  impl-s2-parse: { status: pending, step: Step2, executor: subagent }
  impl-s3-persist: { status: pending, step: Step3, executor: subagent }
  impl-s4-backfill: { status: pending, step: Step4, executor: subagent }
  impl-s5-stats: { status: pending, step: Step5, executor: subagent }
  verify-core: { status: pending, scope: T-S1~S5, executor: subagent }
  cr-func-core: { status: pending, step: Step1-5, executor: subagent }
  impl-s6-desktop-ipc: { status: pending, step: Step6, executor: subagent }
  impl-s7-desktop-view: { status: pending, step: Step7, executor: subagent }
  impl-s8-mobile: { status: pending, step: Step8, executor: subagent }
  verify-ui: { status: pending, scope: T-S6~S7, executor: subagent }
  cr-func-final: { status: pending, step: Step6-8, executor: subagent }
open_must_fix: []
spec_deviations: []
notes: >
  Step 9 (manual_user) 不入 DAG。依赖：s2/s3/s4/s5 依赖 s1；s6 依赖 s5；s7 依赖 s6；
  s8 依赖 s5。s2 与 s3 文件不重叠可并行；s4（新迁移文件+占位已由 s1 登记）与 s5 无冲突。
  工作区遗留用户自身改动（.vscode 删除、AGENTS.md、package-lock.json）——禁止触碰、禁止提交。
