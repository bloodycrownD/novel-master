# token-usage-stats 开发 DAG 编排状态（code-dev-loop）
# spec: docs/Iterations/token-usage-stats/spec.md（execute-ready 已确认）
# branch: feat/token-usage-stats（base aa32a19）

spec_path: docs/Iterations/token-usage-stats/spec.md
prd_path: docs/Iterations/token-usage-stats/prd.md
branch: feat/token-usage-stats
dag_version: 2
wave_plan: [
  [impl-s6-desktop-ipc, impl-s8-mobile],
  [impl-s7-desktop-view],
  [verify-ui],
  [cr-func-final]
]
node_status:
  impl-s1-schema: { status: done, head_sha: d520cfc, executor: subagent, note: "T-S1 42/42" }
  impl-s2-parse: { status: done, head_sha: f814dc7, executor: subagent, note: "T-S2 145/145" }
  impl-s3-persist: { status: done, head_sha: a3e487b, executor: subagent, note: "T-S3 601/601；agent-runner.test 补 savedModels stub" }
  impl-s4-backfill: { status: done, head_sha: abce19b, executor: subagent, note: "T-S4 52+362；keyset 分页 + up 内 ALTER 补列（cr 已接受）" }
  impl-s5-stats: { status: done, head_sha: cf06b46, executor: subagent, note: "T-S5 11/11；快照补 createUsageStatsService（单独 commit）" }
  verify-core: { status: done, executor: main, note: "全量 2139/2142，3 失败为 main 基线既有（vfs 域，基线 worktree 复跑归因）；typecheck 过" }
  cr-func-core: { status: done, func_ready: yes, note: "矩阵 5/5；4 实现细节 deviation 已接受" }
  impl-s6-desktop-ipc: { status: pending, step: Step6, executor: subagent }
  impl-s7-desktop-view: { status: pending, step: Step7, executor: subagent }
  impl-s8-mobile: { status: pending, step: Step8, executor: subagent }
  verify-ui: { status: pending, scope: T-S6~S7, executor: subagent }
  cr-func-final: { status: pending, step: Step6-8, executor: subagent }
open_must_fix: []
spec_deviations:
  - id: DEV-1-listmodels-unrecorded
    status: open
    note: listModels() 不含「未记录」项（UI 侧补齐）；Step 7/8 的模型选择器必须含「未记录」选项，否则 AC-11 断链——cr-func-final 检查项
    owner: impl-s7/impl-s8
notes: >
  Step 9 (manual_user) 不入 DAG。依赖：s6 依赖 s5；s7 依赖 s6；s8 依赖 s5。
  并行节点禁止 git commit，由主代理按节点分组提交。
  工作区遗留用户自身改动（.vscode 删除、AGENTS.md、package-lock.json）——禁止触碰、禁止提交。
