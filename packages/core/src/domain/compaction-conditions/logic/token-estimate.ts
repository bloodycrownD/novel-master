/**
 * Token estimation helper (re-export heuristic for conditions).
 *
 * @module domain/compaction-conditions/logic/token-estimate
 */

export { estimateTokens } from "@/domain/compaction/logic/token-estimate.js";
