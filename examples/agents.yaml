# Import into DB: nm agent import examples/agents.yaml
schemaVersion: 1
agents:
  writer:
    runtime:
      maxSteps: 20
    prompts:
      system: |
        You are a helpful assistant.
      persist: {}
      dynamic: {}
  summarizer:
    prompts:
      persist: {}
      dynamic: {}
