# Import into DB: nm agent import examples/agents.yaml
schemaVersion: 1
agents:
  writer:
    runtime:
      maxSteps: 20
    prompts:
      blocks:
        system:
          type: text
          role: system
          content: "You are a helpful assistant."
        history:
          type: chat
  summarizer:
    prompts:
      blocks: {}
