schemaVersion: 1

name: writer

runtime:
  maxSteps: 20

prompts:
  blocks:
    - name: system
      type: text
      role: system
      content: "You are a helpful assistant."
    - name: abstract
      type: abstract
      content: |
        压缩后的内容如下：
        {{.abstract}}
    - name: history
      type: chat
